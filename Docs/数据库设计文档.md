# MapTools 数据库设计文档

## 概述

本文档描述当前版本数据库设计范围：为简化部署与验证，所有业务数据（轨迹/刷卡/路网等）均直接从项目本地 `data/` 目录读取，不入库；数据库仅用于用户认证（`users` 表）。

## 数据库配置

- **类型**: MySQL 8.0（现有环境）
- **字符集**: utf8mb4；**排序规则**: utf8mb4_unicode_ci；**引擎**: InnoDB
- 测试环境可使用 SQLite 以运行集成测试

## 表结构设计

### 1. 用户表 (users)
```sql
CREATE TABLE users (
    user_id VARCHAR(36) PRIMARY KEY COMMENT '用户ID',
    username VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
    email VARCHAR(100) NOT NULL UNIQUE COMMENT '邮箱',
    password_hash VARCHAR(255) NOT NULL COMMENT '密码哈希',
    full_name VARCHAR(100) COMMENT '全名',
    phone VARCHAR(20) COMMENT '手机号',
    avatar_url VARCHAR(500) COMMENT '头像URL',
    is_active BOOLEAN DEFAULT TRUE COMMENT '是否激活',
    is_admin BOOLEAN DEFAULT FALSE COMMENT '是否管理员',
    last_login_at TIMESTAMP NULL COMMENT '最后登录时间',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_username (username),
    INDEX idx_email (email),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB COMMENT='用户表';
```

## 业务数据说明（不入库）
- 轨迹/刷卡/交易/线路站点/线路关键点/路网：均从 `data/` 读取（TXT/CSV/GeoJSON/PBF 等）。
- 前端底图：天地图 WMTS `vec_w`（栅格切片，仅展示）。
- 匹配用路网：OpenStreetMap 数据离线解析（osmnx/pyrosm），如需缓存建议写入文件（GeoPackage/Parquet）；本版本不入库。

## 后续演进（可选，不在本版本实施）
- 引入 PostGIS 存储 OSM 道路几何与空间索引，加速匹配与统计。
- 将高频查询的原始/派生数据落地为表，以实现审计与分析。

## 备份与安全
- 仅需备份 `users` 表与 `.env`/密钥；业务数据由 `data/` 维护。

## 版本记录
- v0.3.0 (2025-09-18): 精简为“DB 仅 users 表；业务数据走 data/”。
