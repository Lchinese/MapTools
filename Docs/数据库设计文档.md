# MapTools 数据库设计文档

## 概述

本文档详细描述了 MapTools 系统的数据库设计。由于系统直接使用本地出租车数据文件，数据库仅用于存储用户认证相关信息。

## 数据库配置

### 基本信息
- **数据库类型**: MySQL 8.0
- **字符集**: utf8mb4
- **排序规则**: utf8mb4_unicode_ci
- **存储引擎**: InnoDB

### 测试数据库
项目包含专门用于测试的 SQLite 数据库配置，用于运行集成测试，避免影响生产数据。

### 连接配置
```
-- 创建数据库
CREATE DATABASE maptools 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_unicode_ci;

-- 创建用户
CREATE USER 'maptools_user'@'%' IDENTIFIED BY 'maptools_password';
GRANT ALL PRIVILEGES ON maptools.* TO 'maptools_user'@'%';
FLUSH PRIVILEGES;
```

---

## 表结构设计

### 1. 用户表 (users)

存储系统用户信息。

```sql
CREATE TABLE users (
    user_id VARCHAR(36) PRIMARY KEY COMMENT '用户ID',
    username VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
    email VARCHAR(100) NOT NULL UNIQUE COMMENT '邮箱',
    password_hash VARCHAR(255) NOT NULL COMMENT '密码哈希',
    full_name VARCHAR(100) COMMENT '全名',
    avatar_url VARCHAR(500) COMMENT '头像URL',
    is_active BOOLEAN DEFAULT TRUE COMMENT '是否激活',
    is_admin BOOLEAN DEFAULT FALSE COMMENT '是否管理员',
    last_login_at TIMESTAMP NULL COMMENT '最后登录时间',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    
    INDEX idx_username (username),
    INDEX idx_email (email),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB COMMENT='用户表';
```

---

## 数据库使用说明

### 轨迹数据存储

系统中的所有轨迹数据均存储在本地文件系统中，位于 [data](../data) 目录下，按照日期分类存储。系统直接从文件系统中读取轨迹数据，无需数据库存储。

### 用户认证数据存储

数据库仅用于存储用户认证相关信息，包括：
1. 用户基本信息
2. 用户登录凭证
3. 用户权限信息

### 优势

1. **简化架构**：无需维护复杂的轨迹数据表结构
2. **提高性能**：直接读取文件比数据库查询更快速
3. **降低维护成本**：减少数据库维护工作量
4. **易于扩展**：添加新数据只需放入指定目录

---

## 性能优化建议

### 1. 分区策略

```

```

### 2. 查询优化

```

### 3. 缓存策略

- 使用 Redis 缓存热点轨迹数据
- 缓存路网统计信息
- 缓存用户会话信息
- 使用查询结果缓存

---

## 备份和恢复

### 1. 备份策略

```

```

### 2. 恢复策略

```

```

---

## 监控和维护

### 1. 性能监控

```

```

### 2. 定期维护

```

```

---

## 安全考虑

### 1. 数据加密

- 敏感字段使用 AES 加密
- 密码使用 bcrypt 哈希
- 传输使用 TLS 加密

### 2. 访问控制

- 基于角色的权限控制
- 行级安全策略
- API 访问限制

### 3. 审计日志

- 记录所有数据修改操作
- 定期审查访问日志
- 异常行为告警

---

## 版本控制

### 数据库版本管理

使用 Flyway 或 Liquibase 进行数据库版本管理：

```
