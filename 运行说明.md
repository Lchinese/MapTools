# MapTools 轨迹匹配系统 - 运行说明

## 快速启动

### 方法一：使用启动脚本（推荐）
在项目根目录下运行：
```powershell
.\start.ps1
```

### 方法二：手动启动

#### 启动后端服务
```bash
cd BackendService
uv run python main.py
```

#### 启动前端服务
```bash
cd FrontendApp
npm start
```

## 访问地址

- **前端应用**: http://localhost:3000
- **后端API**: http://localhost:8000
- **API文档**: http://localhost:8000/docs
- **健康检查**: http://localhost:8000/api/v1/health

## 系统要求

- Python 3.12+
- Node.js 18+
- MySQL 8.0+（仅用于用户表）
- uv (Python包管理器)

## 数据来源（当前版本）
- 业务数据（轨迹/刷卡/线路/路网）直接从项目本地 `data/` 目录读取，不入库。
- 数据库仅保留 `users` 表用于认证。
- 前端底图使用天地图 WMTS `vec_w`（展示用，栅格切片）。
- 匹配所需道路矢量来源为 OpenStreetMap（Overpass/Geofabrik + osmnx/pyrosm 解析）。

## 故障排除

### 后端启动失败
1. 检查 MySQL 是否可用（仅用户表）
2. 确认 `.env` 数据库连接配置正确
3. 运行 `uv sync` 安装依赖

### 前端启动失败
1. 运行 `npm install` 安装依赖
2. 检查 Node.js 版本是否兼容

### 数据库连接问题
1. 确认 MySQL 服务运行
2. 检查用户名密码配置
3. 如需初始化用户表：
   ```bash
   cd BackendService
   uv run python scripts/init_database.py
   ```

## 开发说明

- 后端使用 FastAPI + SQLAlchemy（仅用户认证入库）
- 前端使用 React + Ant Design + Leaflet
- 底图：天地图 WMTS `vec_w`；路网矢量：OSM
- 状态管理使用 Zustand
