# MapTools 轨迹匹配系统 - 运行说明

## 快速启动

### 方法一：使用启动脚本（推荐）
在项目根目录下运行：
```powershell
.\start.ps1
```

### 方法二：手动启动

#### 启动后端服务
```bash
cd BackendService
uv run python main.py
```

#### 启动前端服务
```bash
cd FrontendApp
npm start
```

## 访问地址

- **前端应用**: http://localhost:3000
- **后端API**: http://localhost:8000
- **API文档**: http://localhost:8000/docs
- **健康检查**: http://localhost:8000/api/v1/health

## 系统要求

- Python 3.12+
- Node.js 18+
- MySQL 8.0+
- uv (Python包管理器)

## 功能特性

### 后端功能
- ✅ 轨迹文件上传（GPX、KML、CSV、TXT）
- ✅ 地图匹配算法（最短距离）
- ✅ 数据库管理（MySQL + SQLAlchemy ORM）
- ✅ RESTful API接口
- ✅ 实时任务状态监控
- ✅ 结果文件下载

### 前端功能
- ✅ 现代化React界面
- ✅ 交互式地图显示
- ✅ 文件拖拽上传
- ✅ 实时进度显示
- ✅ 结果可视化
- ✅ 响应式设计

## 故障排除

### 后端启动失败
1. 检查MySQL服务是否运行
2. 确认数据库连接配置正确
3. 运行 `uv sync` 安装依赖

### 前端启动失败
1. 运行 `npm install` 安装依赖
2. 检查Node.js版本是否兼容

### 数据库连接问题
1. 确认MySQL服务运行
2. 检查用户名密码配置
3. 运行数据库初始化脚本：
   ```bash
   cd BackendService
   uv run python scripts/init_database.py
   ```

## 开发说明

- 后端使用FastAPI + SQLAlchemy
- 前端使用React + Ant Design
- 地图使用Leaflet
- 状态管理使用Zustand
